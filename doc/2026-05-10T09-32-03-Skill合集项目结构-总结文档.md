# Skill 合集项目结构总结文档

## 本次产出

本次将 Skill 合集项目的目录结构建议整理为文档，并保存到当前目录。

主要结论是：当前阶段不需要完整照搬 `antfu/skills` 的自动化维护结构，建议先使用轻量结构启动项目。

## 建议采用的初始结构

```text
low-altitude-skills/
├── README.md
├── AGENTS.md
├── LICENSE.md
└── skills/
    ├── your-first-skill/
    │   ├── SKILL.md
    │   └── references/
    ├── your-second-skill/
    │   ├── SKILL.md
    │   └── references/
    └── your-third-skill/
        ├── SKILL.md
        └── references/
```

## 必要文件

必须保留：

```text
skills/{skill-name}/SKILL.md
```

建议保留：

```text
README.md
AGENTS.md
LICENSE.md
skills/{skill-name}/references/
```

后续按需增加：

```text
package.json
meta.ts
scripts/
sources/
vendor/
instructions/
```

## 下一步计划

下一步可以开始创建实际 Skill 合集项目骨架，包括 `README.md`、`AGENTS.md`、`LICENSE.md` 和 `skills/` 目录。

如果已有 Skill 文件，可以继续把它们迁移到 `skills/{skill-name}/SKILL.md`，并将详细资料拆分到对应的 `references/` 目录中。
