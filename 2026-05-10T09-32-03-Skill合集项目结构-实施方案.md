# Skill 合集项目结构实施方案

## 背景

当前目标是创建一个类似 `antfu/skills` 的 Skill 合集项目，用于集中维护多个自定义 Skill。该类项目的核心不是业务应用，而是把多个 Skill 以统一目录结构、统一入口文件、统一说明文档的方式组织起来，方便后续安装、同步、复用和扩展。

## 最小可用结构

如果当前只是维护几个自己写的 Skill，建议先使用最小结构：

```text
my-skills/
├── README.md
├── LICENSE.md
├── AGENTS.md
└── skills/
    ├── skill-a/
    │   ├── SKILL.md
    │   └── references/
    │       └── xxx.md
    └── skill-b/
        ├── SKILL.md
        └── references/
            └── xxx.md
```

真正必须存在的是：

```text
skills/{skill-name}/SKILL.md
```

`SKILL.md` 是每个 Skill 的入口文件，用来说明 Skill 名称、触发场景、能力范围和引用资料。`references/` 不是强制目录，但建议保留，用来拆分详细规则、示例和背景资料，避免 `SKILL.md` 过长。

## 单个 Skill 推荐结构

```text
skills/vue-style-guide/
├── SKILL.md
├── references/
│   ├── core-patterns.md
│   ├── best-practices.md
│   └── testing.md
└── SYNC.md 或 GENERATION.md
```

其中：

- `SKILL.md`：入口文档，描述什么时候使用该 Skill。
- `references/`：详细参考文档，按主题拆分。
- `SYNC.md`：可选，用于说明该 Skill 是否从外部来源同步。
- `GENERATION.md`：可选，用于说明该 Skill 是否由外部源码、文档或规则生成。

## SKILL.md 示例结构

以下是示意结构，不是业务代码：

```md
---
name: vue-style-guide
description: Vue 项目编码规范、组件拆分、状态管理和测试约定。
metadata:
  author: your-name
  version: "2026.05.10"
---

## 使用场景

当项目涉及 Vue 组件、组合式 API、Pinia、Vue Router 或测试时使用。

## Core References

| Topic | Description | Reference |
|-------|-------------|-----------|
| 组件模式 | 组件拆分、props/emits、组合式 API 约定 | [core-patterns](references/core-patterns.md) |
| 测试规范 | Vitest 和 Vue Test Utils 使用约定 | [testing](references/testing.md) |
```

## 完整项目结构

如果后续希望做到更接近 `antfu/skills` 的完整维护方式，可以逐步扩展为：

```text
my-skills/
├── README.md
├── AGENTS.md
├── LICENSE.md
├── package.json
├── meta.ts
├── scripts/
├── instructions/
│   └── vue.md
├── sources/
│   └── some-project/
├── vendor/
│   └── some-vendor/
└── skills/
    └── {skill-name}/
        ├── SKILL.md
        ├── references/
        ├── GENERATION.md
        └── SYNC.md
```

## 必要目录和文件

必要项：

```text
README.md
skills/
skills/{skill-name}/SKILL.md
```

建议必要项：

```text
AGENTS.md
LICENSE.md
skills/{skill-name}/references/
```

仅在需要自动化同步、生成或检查时再添加：

```text
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
meta.ts
scripts/
sources/
vendor/
instructions/
.gitmodules
```

## 目录职责

`skills/`：最终发布和安装的 Skill 成品目录。

`skills/{name}/SKILL.md`：每个 Skill 的入口文件，必须清楚说明触发场景、能力边界和引用文件。

`skills/{name}/references/`：拆分后的详细知识文件，建议一个主题一个文件。

`sources/`：上游项目源码或文档目录，用来生成自己的 Skill。

`vendor/`：别人已经写好的 Skill 来源目录，用来同步复制。

`instructions/`：给 Agent 使用的生成说明，描述如何从某个上游项目整理成 Skill。

`meta.ts`：集中登记哪些仓库是 source，哪些是 vendor，以及最终输出到哪个 Skill 名称。

`scripts/`：自动 clone、sync、cleanup、check 等脚本目录。

`AGENTS.md`：仓库级协作规则，告诉 Agent 如何维护这些 Skill。

## 推荐落地步骤

第一阶段先采用手写维护版：

```text
my-skills/
├── README.md
├── AGENTS.md
├── LICENSE.md
└── skills/
    ├── your-first-skill/
    │   ├── SKILL.md
    │   └── references/
    ├── your-second-skill/
    │   ├── SKILL.md
    │   └── references/
    └── your-third-skill/
        ├── SKILL.md
        └── references/
```

第二阶段等 Skill 数量变多，或者需要定期从外部文档同步时，再增加：

```text
meta.ts
scripts/
sources/
vendor/
instructions/
```

## 结论

当前最合适的方案是先创建一个轻量 Skill 合集仓库，只保留 `skills/`、`SKILL.md`、`references/`、`README.md`、`AGENTS.md` 和 `LICENSE.md`。这样可以保持结构与 `antfu/skills` 的思想一致，同时避免过早引入自动化脚本、子模块和同步规则带来的维护成本。
